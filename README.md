# 🛒 Shopping Cart Project

Проект "Корзина товаров" с иерархией классов продуктов и системой скидок.

## 🎯 Цель проекта

Реализация объектно-ориентированной модели продуктовой корзины с:
- Иерархией классов продуктов
- Системой скидок через интерфейс
- Вычислением общей стоимости
- Фильтрацией по вегетарианским продуктам

## 📁 Структура проекта
src/main/java/
├── Main.java # Точка входа, демонстрация работы
├── model/ # Пакет с моделями данных
│ ├── Food.java # Абстрактный базовый класс продуктов
│ ├── Meat.java # Класс мясных продуктов
│ ├── Apple.java # Класс яблок с учётом цвета
│ ├── Discountable.java # Интерфейс для скидок
│ └── constants/ # Пакет с константами
│ ├── Colour.java # Константы цветов
│ └── Discount.java # Константы скидок
├── service/ # Пакет с бизнес-логикой
│ └── ShoppingCart.java # Класс корзины покупок

## 📚 Описание классов

### 🍎 **Класс Food (абстрактный)**
Базовый абстрактный класс для всех продуктов.
- **Поля:**
    - `amount` - количество в килограммах (protected)
    - `price` - цена за килограмм (protected)
    - `isVegetarian` - флаг вегетарианского продукта (protected)
- **Методы:** геттеры для всех полей

### 🥩 **Класс Meat**
Наследник `Food`, представляет мясные продукты.
- **Конструктор:** `Meat(int amount, double price)`
- **Особенности:**
    - Автоматически устанавливает `isVegetarian = false`
    - Реализует интерфейс `Discountable` (скидка всегда 0%)
    - Наследует все свойства от `Food`

### 🍏 **Класс Apple**
Наследник `Food`, представляет яблоки.
- **Дополнительное поле:** `colour` (цвет яблок)
- **Конструктор:** `Apple(int amount, double price, String colour)`
- **Особенности:**
    - Автоматически устанавливает `isVegetarian = true`
    - Реализует интерфейс `Discountable`
    - **Скидка 60%** для красных яблок (`colour = "red"`)
    - Для других цветов скидка 0%

### 📋 **Интерфейс Discountable**
Интерфейс для продуктов со скидкой.
- **Метод:** `double getDiscount()` - возвращает размер скидки в процентах

### 🛒 **Класс ShoppingCart**
Класс корзины покупок для работы с продуктами.
- **Поле:** `Food[] items` - массив продуктов
- **Конструктор:** `ShoppingCart(Food[] items)`
- **Методы:**
    - `getTotalPriceWithoutDiscount()` - общая стоимость без скидок
    - `getTotalPriceWithDiscount()` - общая стоимость со скидками
    - `getVegetarianTotalPrice()` - стоимость вегетарианских продуктов

### 🎨 **Класс Colour** (в пакете constants)
Содержит константы цветов.
- `public static final String RED = "red"`
- `public static final String GREEN = "green"`

### 💰 **Класс Discount** (в пакете constants)
Содержит константы скидок.
- `public static final double RED_APPLE = 60.0`


👤 Автор

Азалия